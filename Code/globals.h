struct coordenadas {
    float x, y, z;
    float r, g, b, a;
    float raio;
};

struct ponto {
    float x, y;
    float porcentagemX, porcentagemY;
};

enum situationobject { vivo = 0, pego, solto, ganho };
